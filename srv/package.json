{
  "name": "api-dash",
  "version": "1.0.0",
  "private": true,
  "description": "API_DASH: caché y agregación compartida para dashboards empresariales.",
  "main": "server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test --test-isolation=none test/**/*.test.js"
  },
  "dependencies": {
    "@sap-cloud-sdk/http-client": "^4.3.0",
    "express": "^4.21.2"
  }
}

